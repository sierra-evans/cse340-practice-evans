<%- include('../partials/header') %>
<main>
  <h1>Faculty Directory</h1>
  <p>Browse our faculty members and click on any name to see detailed information including office, email, and department.</p>

  <div class="sort-options">
    <span>Sort by: </span>
    <a href="/faculty?sort=name" class="<%= currentSort === 'name' ? 'active' : '' %>">Name</a>
    <a href="/faculty?sort=department" class="<%= currentSort === 'department' ? 'active' : '' %>">Department</a>
    <a href="/faculty?sort=title" class="<%= currentSort === 'title' ? 'active' : '' %>">Title</a>
  </div>

  <div class="faculty-list">
    <% faculty.forEach(member => { %>
      <div class="faculty-card">
        <h3>
          <a href="/faculty/<%= member.slug %>"><%= member.name %></a>
        </h3>

        <p class="faculty-title"><%= member.title %> - <%= member.department %></p>

        <p class="faculty-office">Office: <%= member.office %></p>
        <p class="faculty-email">
          Email:
          <a href="mailto:<%= member.email %>"><%= member.email %></a>
        </p>
        <p class="faculty-phone">Phone: <%= member.phone %></p>
      </div>
    <% }) %>
  </div>
</main>
<%- include('../partials/footer') %>